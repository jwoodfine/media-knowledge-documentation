# Redirects for content-wiki-documentation
# Articles moved to pointsav-design-system on 2026-05-16 as part of the
# design-system category split (see naming-convention.md §13 Decision #7).
# Format: Hugo-compatible redirect list. The wiki engine emits 301 for
# each `from` path. Language variants are handled by the engine's i18n
# routing; only EN slugs are listed here.

redirects:

  # Category-fit design sweep 2026-08-03 (project-editorial re-derivation of a
  # lost-transcript audit; durable record: .agent/audit/phase1c-documentation-sweep/
  # category-fit-design-2026-08-03.md).
  #
  # totebox-archive: moved systems/ -> infrastructure/. Its content (disk-image/VM
  # packaging, WORM flat-file storage model, Diode+PSP access model, hypervisor
  # relationship) is deployment/storage-shaped per infrastructure/'s charter, not an
  # operating-system description — os-family-overview.md already carries the
  # systems/-composition role for the OS family, and totebox-os.md already carries the
  # OS-itself description. Slug and category-field both updated; MOC entries moved from
  # systems/_index.md to infrastructure/_index.md (EN+ES).
  - from: /systems/totebox-archive
    to: /wiki/totebox-archive

  # app-orchestration-command-branch-model, app-orchestration-command-publication-flow:
  # moved systems/ -> applications/ (this note is historical — see the superseding
  # note below; kept for the from: path history).
  # 2026-08-25: both articles were subsequently retired entirely (commit 3f571a8,
  # 2026-08-06) — they misattributed this workspace's own promote.sh/cluster-branch
  # publication pipeline to the app-orchestration-command product, which does none of
  # that (its real modules are invite/pairing/fleet/personnel/license/routing/child).
  # The real mechanism is already documented accurately in five-stage-supply-chain.md.
  # These two `to:` targets accordingly now point there instead of the dead
  # applications/ slug, matching how every other inbound reference was repointed at
  # retraction time.
  - from: /systems/app-orchestration-command-branch-model
    to: /wiki/five-stage-supply-chain
  - from: /systems/app-orchestration-command-publication-flow
    to: /wiki/five-stage-supply-chain

  # quick-start retired into getting-started (both reference/, both orphaned
  # quality:stub articles with no inbound wikilinks from any curated MOC and
  # overlapping onboarding purpose/content — guide-catalog, ppn-small-business-compute,
  # and machine-based-auth links present in both). getting-started kept as canonical
  # (broader "Where to start" scope, incl. GIS/data); quick-start's Prerequisites +
  # First steps sections merged in as new sections. aliases: [quick-start] added to
  # getting-started.md + .es.md.
  - from: /reference/quick-start
    to: /wiki/getting-started

  # service-fs cluster consolidation 2026-07-31 (content-matrix merge-candidate work):
  # service-fs-architecture.md and service-fs-security-compliance.md were genuine
  # redundancy (same subject — WORM ledger architecture, Merkle chains, Rekor anchoring,
  # seL4 target — described twice from architecture vs. compliance angles). Merged into
  # one canonical services/service-fs.md, which carries
  # aliases: [service-fs-architecture, service-fs-security-compliance] for wikilink
  # resolution. service-fs-data-lake.md was investigated and kept SEPARATE — it
  # describes a genuinely different subject (the GIS flat-file landing-zone directory
  # convention, not the WORM ledger service) and was independently corrected for its
  # own unhedged-claim defect, not merged.
  - from: /services/service-fs-architecture
    to: /wiki/service-fs
  - from: /services/service-fs-security-compliance
    to: /wiki/service-fs

  # os-console cluster consolidation 2026-07-31 (content-matrix merge-candidate work):
  # console-os.md, os-console-architecture.md, and os-console-platform.md were genuine
  # fragmentation — all three described the same product at overlapping technical
  # depths, with real inconsistencies (differing F-key/cartridge-state tables; the
  # architecture article's own "four active" claim was incomplete against the
  # platform's project registry, which shows 5 Active cartridges). Merged into one
  # canonical systems/os-console.md, which also fixes console-os.md's reversed slug
  # (the file was slugged "console-os" but its own body used "os-console" throughout).
  # Carries aliases: [console-os, os-console-architecture, os-console-platform,
  # topic-os-console-architecture] for wikilink resolution.
  # os-console-totebox-browser.md was investigated and kept SEPARATE — a genuinely
  # distinct browser-analogy explainer, not a duplicate.
  - from: /systems/console-os
    to: /wiki/os-console
  - from: /systems/os-console-architecture
    to: /wiki/os-console
  - from: /systems/os-console-platform
    to: /wiki/os-console

  # PPN cluster consolidation 2026-08-01 (content-matrix merge-candidate work):
  # Of 11 flagged PPN articles, only ONE genuine duplicate pair was found on full read —
  # pointsav-private-network.md and ppn-mesh-architecture.md described the identical
  # subject (topology, key custody, WireGuard fabric, Mesh Fusion, F8 Terminal, PPN/MBA
  # independence) in the same structure. The other 9 articles turned out legitimately
  # complementary (mesh deployment/custody vs. mesh protocol/technical, per-node
  # hypervisor mechanism vs. planned cross-node fabric, VM control-plane stack vs. its
  # isolation/security angle, wire-format deep dive, seL4 roadmap, product pitch) —
  # the audit's "over-covered, heavily fragmented" framing overstated the problem for
  # 9 of 11. Merged pointsav-private-network.md into ppn-mesh-architecture.md, which
  # also fixes 2 real factual defects the retiring article carried: an address range
  # (10.50.0.0/24) contradicted by real code and by sovereign-mesh.md's correct
  # 10.8.0.0/24, and a peer-registry-holder name (route-network-admin) that does not
  # exist anywhere in the codebase (real name: os-network-admin). Carries
  # aliases: [pointsav-private-network] for wikilink resolution. Two real cross-article
  # discrepancies (a UDP port number contradiction between 3 wiki articles and the
  # more-built app-network-admin crate; a hypervisor-roadmap sequencing inconsistency)
  # were found but NOT resolved — flagged for engineering clarification, not an
  # editorial call.
  - from: /infrastructure/pointsav-private-network
    to: /wiki/ppn-mesh-architecture

  # GIS/location-intelligence cluster consolidation 2026-08-01 (content-matrix
  # merge-candidate work, final of 5 flagged clusters). Of 5 candidate articles, only
  # services/pointsav-gis-engine.md was genuinely redundant — its own body said "the
  # engine's core logic resides in app-orchestration-gis," and its remaining content
  # duplicated substrate/location-intelligence-substrate.md at lower fidelity. Merged
  # into location-intelligence-substrate.md (carries aliases: [pointsav-gis-engine]).
  # The other 3 (location-intelligence-platform, location-intelligence-substrate,
  # app-orchestration-gis) confirmed legitimately distinct facets — product overview,
  # technical foundation, analytics engine — not merged.
  # A more serious defect surfaced while verifying claims against real code: 4 of the
  # 5 articles cited [[co-location-methodology]] as "the retail cluster tier scoring
  # methodology," but that slug resolves to architecture/co-location-methodology.md —
  # a real, unrelated article about data-center FACILITY co-location, not retail
  # anchors. No article for the real retail-tier methodology existed under any slug.
  # Worse, the one place a scoring mechanism WAS described in detail
  # (app-orchestration-gis.md) described a Haversine linear-decay formula the real
  # SCORING-METHODOLOGY.md (V3, verified) confirms was removed and replaced by a
  # 4-tier predicate-gate system — and 2 more articles in the cluster each described
  # the mechanism differently, with neither matching the real system. Fixed by
  # creating substrate/retail-co-location-tier-methodology.md as the single source of
  # truth (real facts only, from the verified V3 methodology doc) and repointing all
  # citing articles to it instead of re-describing the mechanism themselves.
  - from: /services/pointsav-gis-engine
    to: /wiki/location-intelligence-substrate

  # scaling-coordinated-development three-way duplicate resolved 2026-07-28 (diagnosed
  # 2026-07-09, execution blocked by the permission system until operator sign-off this
  # session). architecture/...-totebox-archives.md and systems/...-totebox-archives.md
  # had the identical slug in two categories (a real bug — slugs must be globally
  # unique); systems/...-sovereign-archives.md was a leaked draft with
  # forbidden_terms_cleared: false, never meant to publish live. Both removed;
  # systems/scaling-coordinated-development-totebox-archives.md is sole canonical
  # (already carries aliases: [scaling-coordinated-development-sovereign-archives]).
  - from: /systems/scaling-coordinated-development-sovereign-archives
    to: /wiki/scaling-coordinated-development-totebox-archives

  - from: /architecture/scaling-coordinated-development-totebox-archives
    to: /wiki/scaling-coordinated-development-totebox-archives

  # research/ category retired 2026-07-15 (naming-convention.md §13 Decision #13) —
  # its one real article relocated to reference/; slug unchanged, path only.
  - from: /research/preprint-notice-convention
    to: /wiki/preprint-notice-convention

  # os-totebox.md stub retired 2026-07-18 — redundant with totebox-os.md, which already
  # claims `os-totebox` as an alias and is the complete, correctly-hedged canonical
  # article. 12 real inbound [[os-totebox]] wikilinks were resolving to the 4-sentence
  # stub instead of the complete article before this fix; retiring the stub file lets
  # the alias resolve correctly. Path redirect kept in case any external link hit the
  # stub's URL directly.
  - from: /systems/os-totebox
    to: /wiki/totebox-os

  # os-privategit-workbench.md retired 2026-07-30 — redundant duplicate of
  # applications/app-privategit-workbench.md (same crate, same architecture, near-
  # identical text). This copy's own slug incorrectly used an os- prefix for an app-*
  # crate; archived to systems/.archive/ per the newly-adopted archive-before-delete
  # convention rather than deleted outright.
  - from: /systems/os-privategit-workbench
    to: /wiki/app-privategit-workbench

  # Batch 1 — Foundation token docs → design.pointsav.com.
  # Destination corrected 2026-07-28: the original /foundations/<slug> targets
  # (set 2026-05-16, Decision #7) are dead — design.pointsav.com was rebuilt since
  # (new site-wide IA, confirmed live: /tokens, /elements/<name>/overview, etc.) and
  # never carried the old paths forward. All 4 corrected targets verified live via
  # direct fetch before this change (real matching token content, not a 404).
  - from: /design-system/design-color
    to: https://design.pointsav.com/elements/color/overview

  - from: /design-system/design-typography
    to: https://design.pointsav.com/elements/typography/overview

  - from: /design-system/design-spacing
    to: https://design.pointsav.com/elements/spacing/overview

  - from: /design-system/design-motion
    to: https://design.pointsav.com/elements/motion/overview

  # Batch 2 — Component guides → pointsav-design-system/components/<name>/usage.md
  # Repointed 2026-08-09: /guide 404s on the live local-design server (port 9094),
  # /usage is the real serving path (dtcg-vault has only ever built usage.md, not
  # guide.md — flagged by Command 2026-08-02, verified directly here before fixing,
  # every one of these 16 confirmed 200 on /usage before the swap).
  - from: /design-system/guide-component-badge
    to: https://design.pointsav.com/components/badge/usage

  - from: /design-system/guide-component-breadcrumb
    to: https://design.pointsav.com/components/breadcrumb/usage

  - from: /design-system/guide-component-button
    to: https://design.pointsav.com/components/button/usage

  - from: /design-system/guide-component-checkbox
    to: https://design.pointsav.com/components/checkbox/usage

  - from: /design-system/guide-component-citation-authority-ribbon
    to: https://design.pointsav.com/components/citation-authority-ribbon/usage

  - from: /design-system/guide-component-freshness-ribbon
    to: https://design.pointsav.com/components/freshness-ribbon/usage

  - from: /design-system/guide-component-home-grid
    to: https://design.pointsav.com/components/home-grid/usage

  - from: /design-system/guide-component-input-text
    to: https://design.pointsav.com/components/input-text/usage

  - from: /design-system/guide-component-link
    to: https://design.pointsav.com/components/link/usage

  - from: /design-system/guide-component-navigation-bar
    to: https://design.pointsav.com/components/navigation-bar/usage

  - from: /design-system/guide-component-notification
    to: https://design.pointsav.com/components/notification/usage

  - from: /design-system/guide-component-research-trail-footer
    to: https://design.pointsav.com/components/research-trail-footer/usage

  - from: /design-system/guide-component-select
    to: https://design.pointsav.com/components/select/usage

  - from: /design-system/guide-component-surface
    to: https://design.pointsav.com/components/surface/usage

  - from: /design-system/guide-component-switch
    to: https://design.pointsav.com/components/switch/usage

  - from: /design-system/guide-component-tab
    to: https://design.pointsav.com/components/tab/usage

  # Batch 2 wiki-surface docs — REMOVED 2026-07-28. These 3 redirects were dead code:
  # the engine only consults redirects.yaml when no live article exists at that slug
  # (app-mediakit-knowledge src/app.rs, alias-then-redirect fallback), and all 3 slugs
  # have live, current, `status: active` articles — wiki-component-library.md,
  # wiki-dark-mode.md, wiki-typography-system.md — that document THIS wiki's own
  # rendering layer (page components, dark-mode implementation, type system), not
  # generic design-system specs. Verified by reading all 3 bodies directly, not assumed
  # from file size. These are not the Decision #7 "moved to pointsav-design-system"
  # husks the redirects implied; they never should have been listed for that move.

  # Batch 3 — Spatial components → pointsav-design-system/components/<name>/usage.md
  # Repointed 2026-08-09, same guide→usage fix and verification method as Batch 2 above
  # — country-filter-chips/map-side-drawer/map-stats-panel all confirmed 200 on /usage.
  - from: /design-system/country-filter-chips
    to: https://design.pointsav.com/components/country-filter-chips/usage

  - from: /design-system/map-side-drawer
    to: https://design.pointsav.com/components/map-side-drawer/usage

  - from: /design-system/map-stats-panel
    to: https://design.pointsav.com/components/map-stats-panel/usage

  # NOT repointed 2026-08-09: unlike every other entry in this file, climate-zone-tokens
  # and zoom-tier-reveal-pattern 404 on BOTH /guide and /usage — the guide→usage fix that
  # worked for every other component doesn't apply here. Real current location unknown;
  # flagged to project-design rather than guessed at (NEXT.md, 2026-08-09).
  - from: /design-system/climate-zone-tokens
    to: https://design.pointsav.com/components/climate-zone-tokens/guide

  - from: /design-system/zoom-tier-reveal-pattern
    to: https://design.pointsav.com/components/zoom-tier-reveal-pattern/guide

  # Batch 3 — Accessibility docs → pointsav-design-system/docs/accessibility/
  # NOT repointed 2026-08-09: both 404 under /accessibility/<slug> as well as the
  # working /accessibility/overview path — real current location unknown, same as the
  # 2 components above; flagged to project-design rather than guessed at.
  - from: /design-system/neurodiversity-typography-standards
    to: https://design.pointsav.com/accessibility/neurodiversity-typography-standards

  - from: /design-system/properties-panel-accessibility
    to: https://design.pointsav.com/accessibility/properties-panel-accessibility

  - from: /design-system/spatial-tree-accessibility
    to: https://design.pointsav.com/accessibility/spatial-tree-accessibility

  - from: /design-system/viewport-3d-accessibility
    to: https://design.pointsav.com/accessibility/viewport-3d-accessibility

  # Substrate supersession — slm-tiered-substrate superseded by soft-slm-tiered-gateway (2026-06-09)
  - from: /substrate/slm-tiered-substrate
    to: /wiki/soft-slm-tiered-gateway

  # Batch A1a — Phase C category redesign, 2026-07-03: architecture/ split into
  # security/ + ai/ (proposal-documentation.md §4a). Slugs unchanged; path only.
  - from: /architecture/capability-based-security
    to: /wiki/capability-based-security

  - from: /architecture/machine-based-auth
    to: /wiki/machine-based-auth

  - from: /architecture/crypto-attestation
    to: /wiki/crypto-attestation

  - from: /architecture/cryptographic-ledgers
    to: /wiki/cryptographic-ledgers

  - from: /architecture/personnel-permissions
    to: /wiki/personnel-permissions

  - from: /architecture/diode-standard
    to: /wiki/diode-standard

  - from: /architecture/five-stage-supply-chain
    to: /wiki/five-stage-supply-chain

  - from: /architecture/verification-surveyor
    to: /wiki/verification-surveyor

  - from: /architecture/sel4-capability-topology
    to: /wiki/sel4-capability-topology

  - from: /architecture/pre-commit-defense-in-depth
    to: /wiki/pre-commit-defense-in-depth

  - from: /architecture/data-sovereignty-telemetry
    to: /wiki/data-sovereignty-telemetry

  - from: /architecture/identity-ledger-schema-design
    to: /wiki/identity-ledger-schema-design

  - from: /architecture/genesis-protocol
    to: /wiki/genesis-protocol

  - from: /architecture/doorman-protocol
    to: /wiki/doorman-protocol

  - from: /architecture/sovereign-ai-routing
    to: /wiki/sovereign-ai-routing

  - from: /architecture/zero-container-inference
    to: /wiki/zero-container-inference

  - from: /architecture/decode-time-constraints
    to: /wiki/decode-time-constraints

  - from: /architecture/pointsav-llm
    to: /wiki/pointsav-llm

  - from: /architecture/slm-stack-architecture
    to: /wiki/slm-stack-architecture

  - from: /architecture/learning-datagraph-architecture
    to: /wiki/learning-datagraph-architecture

  - from: /architecture/elastic-compute-lora-training-pipeline
    to: /wiki/elastic-compute-lora-training-pipeline

  - from: /architecture/flow-quality-architecture
    to: /wiki/flow-quality-architecture

  - from: /architecture/topic-tiered-entity-extraction-architecture
    to: /wiki/tiered-entity-extraction-architecture

  # Batch A1b — Phase C category redesign, 2026-07-03: architecture/ rebalancing
  # into infrastructure/ + systems/ + substrate/ + services/ (proposal-documentation.md
  # §4a). Slugs unchanged; path only.
  - from: /architecture/ppn-architecture-overview
    to: /wiki/ppn-architecture-overview

  - from: /architecture/ppn-command-protocol
    to: /wiki/ppn-command-protocol

  - from: /architecture/ppn-distributed-vm-fabric
    to: /wiki/ppn-distributed-vm-fabric

  - from: /architecture/ppn-hypervisor-resource-pool
    to: /wiki/ppn-hypervisor-resource-pool

  - from: /architecture/ppn-mesh-architecture
    to: /wiki/ppn-mesh-architecture

  - from: /architecture/ppn-small-business-compute
    to: /wiki/ppn-small-business-compute

  - from: /architecture/ppn-tenant-vm-isolation
    to: /wiki/ppn-tenant-vm-isolation

  - from: /architecture/ppn-three-path-architecture
    to: /wiki/ppn-three-path-architecture

  - from: /architecture/ppn-vm-resource-pool
    to: /wiki/ppn-vm-resource-pool

  - from: /architecture/spot-vm-lifecycle-kill-switch
    to: /wiki/spot-vm-lifecycle-kill-switch

  - from: /architecture/os-console-architecture
    to: /wiki/os-console

  - from: /architecture/topic-os-console-totebox-browser
    to: /wiki/os-console-totebox-browser

  - from: /architecture/os-totebox-service-pd-model
    to: /wiki/os-totebox-service-pd-model

  - from: /architecture/os-totebox-sovereign-archive
    to: /wiki/os-totebox-sovereign-archive

  - from: /architecture/topic-sel4-unikernel-substrate
    to: /wiki/sel4-unikernel-substrate

  - from: /architecture/service-pointsav-link
    to: /wiki/service-pointsav-link

  # Batch A1b collision resolutions — Phase C category redesign, 2026-07-03:
  # duplicate copies retired in favor of the specialist-category copy
  # (proposal-documentation.md §4d). Same slug in both locations; path only.
  # 2026-08-25: both articles were subsequently retired entirely (commit 3f571a8,
  # 2026-08-06) — misattributed this workspace's own promote.sh pipeline to
  # app-orchestration-command. Repointed to the real, accurate replacement,
  # five-stage-supply-chain.md, matching every other inbound reference fixed at
  # retraction time.
  - from: /architecture/app-orchestration-command-branch-model
    to: /wiki/five-stage-supply-chain

  - from: /architecture/app-orchestration-command-publication-flow
    to: /wiki/five-stage-supply-chain

  - from: /architecture/os-console-platform
    to: /wiki/os-console

  - from: /architecture/input-machine
    to: /wiki/input-machine

  - from: /architecture/collab-via-passthrough-relay
    to: /wiki/collab-via-passthrough-relay

  - from: /architecture/customer-tier-catalog-pattern
    to: /wiki/customer-tier-catalog-pattern

  - from: /architecture/editorial-pipeline-three-stages
    to: /wiki/editorial-pipeline-three-stages

  - from: /applications/location-intelligence-ux
    to: /wiki/location-intelligence-ux

  # language-protocol-substrate slug-collision resolution, 2026-07-03 (editorial call,
  # see cleanup-log.md): two DIFFERENT articles shared this slug. substrate/ (platform
  # editorial-infrastructure article, cited by ~35 wikilinks corpus-wide) keeps the slug
  # unchanged. architecture/ (Foundry-internal draft-routing mechanism, zero inbound
  # wikilinks under this slug) renamed to editorial-draft-routing-protocol.
  - from: /architecture/language-protocol-substrate
    to: /wiki/editorial-draft-routing-protocol

  # Batch A1d — Phase C category redesign, 2026-07-03: topic-* prefix batch merges
  # and renames (proposal-documentation.md §4c). Four 2026-06-20 duplicates merged
  # into their 2026-06-13 unprefixed counterparts; six sole articles renamed to drop
  # the legacy topic- prefix (two of the six were prefix-drops only, already in their
  # final category from batches A1a/A1b/A1c; two were collision merge-checks against
  # an incumbent unprefixed slug).
  - from: /architecture/topic-crypto-license-sales-architecture
    to: /wiki/crypto-license-sales-architecture

  - from: /architecture/topic-os-console-architecture
    to: /wiki/os-console

  - from: /architecture/topic-ppn-small-business-compute
    to: /wiki/ppn-small-business-compute

  - from: /architecture/topic-software-distribution-substrate
    to: /wiki/software-distribution-substrate

  - from: /systems/topic-os-console-totebox-browser
    to: /wiki/os-console-totebox-browser

  - from: /architecture/topic-three-binary-architecture
    to: /wiki/three-binary-architecture

  - from: /substrate/topic-sel4-unikernel-substrate
    to: /wiki/sel4-unikernel-substrate

  - from: /ai/topic-tiered-entity-extraction-architecture
    to: /wiki/tiered-entity-extraction-architecture

  # Merge-check: topic-capability-geometry (architecture/) was the same subject as the
  # pre-existing substrate/capability-geometry (already the target of ~8 corpus-wide
  # wikilinks, though its frontmatter still carried a stale foundry-draft-v1 schema from
  # an earlier drafting pass). The properly-schemaed, better-edited topic- body replaces
  # the stale draft content at substrate/capability-geometry; schema corrected to
  # foundry-doc-v1 in the same change.
  - from: /architecture/topic-capability-geometry
    to: /wiki/capability-geometry

  # Merge-check: topic-private-git-paid-customer-endpoint (architecture/) was the same
  # subject as the pre-existing services/private-git-paid-customer-endpoint. One unique
  # fact (token-introspection endpoint) merged into the incumbent; topic- copy retired.
  - from: /architecture/topic-private-git-paid-customer-endpoint
    to: /wiki/private-git-paid-customer-endpoint

  # Batch — Phase C re-categorization, CROSS-REPO move, batch D2 (2026-07-03).
  # 11 articles moved to media-knowledge-projects; 2 thin-stub duplicates retired in
  # favour of the pre-existing, more complete projects.woodfinegroup.com canonical copy.
  # NOTE: 2 further items from the original 13-item move list were NOT moved — flagged
  # as unresolved collisions/content-mismatches and left in place in this repo. See the
  # cleanup-log.md entry dated 2026-07-03 for the comparison write-up:
  #   - architecture/co-location-methodology.md (homonym collision — documentation's
  #     article is about hardware/data-centre co-location, not retail co-location; the
  #     existing projects site-selection/co-location-methodology.md is a different,
  #     unrelated subject with the same slug)
  #   - architecture/regional-market-definition.md (substantial subject overlap with
  #     projects markets/about-regional-markets-system.md and about-regional-markets.md)
  #     — RESOLVED 2026-08-03: confirmed zero inbound wikilinks anywhere in this wiki
  #     and the article's own body deferred to the projects wiki three times already.
  #     Moved intact as its own distinct article (not merged into the stale
  #     about-regional-markets-system.md) to markets/regional-market-definition.md; see
  #     the redirect entry near the end of this file.

  - from: /architecture/co-location-tiering-scoring
    to: https://projects.woodfinegroup.com/wiki/co-location-tiering-scoring

  - from: /architecture/trade-area-methodology
    to: https://projects.woodfinegroup.com/wiki/trade-area-methodology

  - from: /architecture/development-regions
    to: https://projects.woodfinegroup.com/wiki/development-regions

  - from: /architecture/spend-population-provenance
    to: https://projects.woodfinegroup.com/wiki/spend-population-provenance

  - from: /architecture/3d-asset-tokens
    to: https://projects.woodfinegroup.com/wiki/3d-asset-tokens

  - from: /reference/bim-aec-muscle-memory
    to: https://projects.woodfinegroup.com/wiki/bim-aec-muscle-memory

  - from: /reference/bim-design-philosophy
    to: https://projects.woodfinegroup.com/wiki/bim-design-philosophy

  - from: /reference/bim-market-context
    to: https://projects.woodfinegroup.com/wiki/bim-market-context

  - from: /reference/bim-token-taxonomy
    to: https://projects.woodfinegroup.com/wiki/bim-token-taxonomy

  - from: /reference/bim-token-three-layers
    to: https://projects.woodfinegroup.com/wiki/bim-token-three-layers

  - from: /reference/bim-token-what-it-is
    to: https://projects.woodfinegroup.com/wiki/bim-token-what-it-is

  # Retirements — documentation's copy was a thin stub; projects' pre-existing article
  # is canonical and more complete. Redirect to the projects article, not a new copy.
  - from: /architecture/location-intelligence-archetypes
    to: https://projects.woodfinegroup.com/wiki/location-intelligence-archetypes

  - from: /reference/open-bim-regulatory-acceptance
    to: https://projects.woodfinegroup.com/wiki/open-bim-regulatory-acceptance

  # architecture/co-location-methodology.md renamed to
  # architecture/hardware-co-location-methodology.md 2026-08-03 — disambiguates from
  # substrate/retail-co-location-tier-methodology.md, a homonym-collision subject in
  # this same wiki (see the 2026-08-01 GIS/location-intelligence consolidation note
  # above). `aliases: [co-location-methodology]` on the EN article also resolves
  # `[[co-location-methodology]]` wikilinks; this redirect covers direct path hits.
  - from: /architecture/co-location-methodology
    to: /wiki/hardware-co-location-methodology

  # architecture/regional-market-definition.md moved 2026-08-03 to the projects wiki —
  # cross-wiki content move (confirmed orphaned here: zero inbound wikilinks in this
  # wiki, and its own body already deferred to the projects wiki three times). Landed
  # as its own distinct article at markets/regional-market-definition.md, cross-linked
  # with (not merged into) the pre-existing markets/about-regional-markets-system.md.
  - from: /architecture/regional-market-definition
    to: https://projects.woodfinegroup.com/wiki/regional-market-definition

  # reference -> background category split 2026-09-06. `reference`'s own audience note
  # ("consulted, not browsed") was contradicted by ten general-computing explainer
  # articles sitting alongside the genuine consulted material (glossary, catalogues,
  # orientation, editorial standards). The explainers describe nothing PointSav built —
  # they are the field's own vocabulary, written to be read through — so they moved to
  # a new `background` category (categories.yaml order 16). Articles are canonically
  # served at /wiki/<slug>, which does not move; these entries cover the secondary
  # /wiki/<category>/<slug> path form, per the convention every prior category move in
  # this file follows.
  - from: /reference/application-programming-interface
    to: /wiki/application-programming-interface
  - from: /reference/computer-appliance
    to: /wiki/computer-appliance
  - from: /reference/edge-computing
    to: /wiki/edge-computing
  - from: /reference/fog-computing
    to: /wiki/fog-computing
  - from: /reference/just-enough-operating-system
    to: /wiki/just-enough-operating-system
  - from: /reference/lightweight-linux-distribution
    to: /wiki/lightweight-linux-distribution
  - from: /reference/security-through-obscurity
    to: /wiki/security-through-obscurity
  - from: /reference/user-experience-design
    to: /wiki/user-experience-design
  - from: /reference/user-interface-design
    to: /wiki/user-interface-design
  - from: /reference/virtual-appliance
    to: /wiki/virtual-appliance
